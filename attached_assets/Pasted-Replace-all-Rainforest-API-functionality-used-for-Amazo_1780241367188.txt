Replace all Rainforest API functionality used for Amazon bestseller research with SerpApi.

Requirements:

1. Remove all Rainforest API dependencies, environment variables, services, utilities, imports, and references.

   * Delete any usage of RAINFOREST_API_KEY.
   * Remove Rainforest-specific request logic and response parsing.

2. Add SerpApi support using:

   * Environment variable: SERPAPI_API_KEY
   * Official SerpApi Amazon Search endpoint.

3. Create a reusable service:

   * Name: amazonResearchService
   * Purpose: Search Amazon Books and return structured bestseller data.

4. Input:

   * topic (string)
   * maxResults (number, default 20)

5. Search behavior:

   * Search Amazon Books for the supplied topic.
   * Example queries:

     * "ADHD books"
     * "investing books"
     * "artificial intelligence books"
   * Restrict results to books whenever possible.

6. Normalize returned data into this format:

```ts
{
  title: string;
  author: string;
  rating: number | null;
  reviewCount: number | null;
  price: string | null;
  amazonUrl: string;
  thumbnail: string | null;
  bestsellerBadge: boolean;
  rank: number;
}
```

7. Sort results using:

   * Bestseller status first
   * Highest review count
   * Highest rating

8. Return top N results.

9. Add robust error handling:

   * Invalid API key
   * Rate limits
   * Empty search results
   * API failures
   * Network failures

10. Create TypeScript interfaces for:

    * Raw SerpApi response
    * Normalized book result

11. Add logging:

    * Search topic
    * Number of results returned
    * API errors

12. Update all existing Amazon research flows to use the new service without changing the UI.

13. Preserve all existing functionality that depends on Amazon bestseller research:

    * Competitive intelligence
    * Bestseller analysis
    * Market research
    * Similar books discovery
    * Book positioning research

14. If any component currently expects Rainforest data, adapt it to consume the normalized structure above.

15. Add clear comments documenting:

    * SerpApi request flow
    * Mapping logic
    * Error handling

16. Store the API key in Replit Secrets:

```env
SERPAPI_API_KEY=
```

17. Ensure the implementation is production-ready, strongly typed, modular, and follows the existing project architecture.

18. Before making changes, search the codebase for:

* rainforest
* RAINFOREST_API_KEY
* amazon bestseller
* amazon search
* product research

Then refactor all affected files accordingly.
